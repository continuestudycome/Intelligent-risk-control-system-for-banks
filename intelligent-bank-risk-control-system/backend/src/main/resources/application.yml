spring:
  application:
    name: backend
  datasource:
    url: jdbc:mysql://localhost:3306/bank_risk_control?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.risk-control-system.backend.entity
